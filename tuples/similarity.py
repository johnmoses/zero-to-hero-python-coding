""" 
Similarity
"""

letters = ('a', 'b', 'c', 'd', 'e')
numbers = (1, 2, 3, 4, 5)

for index in range(len(letters)):
    print(letters[index])

for index in range(len(numbers)):
    print(numbers[index])
